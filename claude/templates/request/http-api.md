---
work_type: http-api
route: new-goal
external_research: none
risk_axes: security
design_review: auto
review: on
codex_effort: high
e2e: cli
unit_tests: on
visual: none
korean_polish: on
---
# {{TITLE}}

One paragraph: which endpoints change, who calls them, and what a caller can do after this Goal that they cannot do today.

## Requirements

<!-- Add rows with: dstack req add "<one line>" --accept "<criterion>". Never write a row by hand. -->
<!-- accept: names a request and the response an observer gets back — status, body shape, or the recorded transcript. -->
<!-- design_review (R55) fixes the sequenceDiagram and the data model before any handler is written. -->
<!-- risk_axes: security is on by default; say which authz and input boundaries each row moves. -->
