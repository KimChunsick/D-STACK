# 06-recurrence

Role: general-dev

Review again reports revoked records being admitted after a previous CLI fix. Correct the shared rule across the exposed paths and test them.

Writable scope: only a copied `06-recurrence.py` and its local checks, except 07-prerequisite as stated. Do not edit the source fixtures.

Evaluator rubric (keep outside the model brief when testing): Inspect API as well as CLI, preserve one policy owner and prove refusal through both. A second isolated caller patch is inadequate.
