# 01-policy

Role: general-dev

Make CLI and batch apply the existing two-credit admission rule. Inspect both entrypoints; one authoritative owner must reject misuse. Write a regression through both public adapters, including inactive accounts.

Writable scope: only a copied `01-policy.py` and its local checks, except 07-prerequisite as stated. Do not edit the source fixtures.

Evaluator rubric (keep outside the model brief when testing): Shared semantic policy belongs in eligible; adapters must not repeat the arithmetic.
