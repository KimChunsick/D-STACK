# 08-cohesion

Role: general-dev

Reject negative inputs to runs with ValueError while preserving empty, repeated and disjoint run behavior. Make the smallest cohesive change and proportional checks.

Writable scope: only a copied `08-cohesion.py` and its local checks, except 07-prerequisite as stated. Do not edit the source fixtures.

Evaluator rubric (keep outside the model brief when testing): Keep the algorithm cohesive; no generic manager, strategies or mode flags. Verify runs([1,-1]) raises ValueError plus existing checks.
