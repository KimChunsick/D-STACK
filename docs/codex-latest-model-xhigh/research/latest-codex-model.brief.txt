Context: A maintainer's Claude Code skills invoke OpenAI Codex CLI non-interactively via `codex exec`.
Currently one skill pins `-m gpt-5.5 -c model_reasoning_effort="xhigh"`; another relies on ~/.codex/config.toml defaults.
Installed CLI: codex-cli 0.143.0. Date: 2026-07-10.

Goal being researched: pin BOTH skills to the LATEST available OpenAI model for Codex CLI with reasoning effort "xhigh".

Questions:
1. As of July 2026, what is the latest/most capable OpenAI model usable in Codex CLI (`codex exec -m <model>`)? Is gpt-5.5 still the latest, or has a newer model (e.g. gpt-5.5-codex, gpt-6, o-series) been released? Give the exact model slug accepted by `-m`.
2. What reasoning effort values are valid for that model (`model_reasoning_effort`)? Is "xhigh" supported on it?
3. Any evidence AGAINST pinning the newest model at xhigh for adversarial code review / research delegation (cost, latency, availability, deprecation churn, rate limits)?
4. What is the default model of codex-cli 0.143.x when no -m/config is given?
