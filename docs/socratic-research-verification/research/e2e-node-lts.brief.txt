Research question: As of 2026-08, which Node.js release lines are in Active LTS, which
are in Maintenance, and what are their scheduled End-of-Life dates? Also: under the
current release cadence policy, which release line is next to enter Active LTS and when?

Context: needed for pinning a runtime baseline in tooling configuration. Note anything
contested, recently changed, or commonly misquoted about the schedule.

Data-checkability note for the ledger: version numbers, phase boundaries, and EOL dates
are checkable against the official Node.js release schedule (the nodejs/Release
repository publishes a machine-readable schedule.json). Enumerate hypotheses and ledger
rows accordingly, and flag as deferred executable checks anything the caller should
verify against that primary source.
