## Carried decisions — Round 003
- F1–F9: fixes stand as sharpened — F8 deepened by F11, F9 replaced by F12's always-delta
  rule, the leaf class gains F14 (brief content) and F15 (trap ordering).
- F10 (high, confused-deputy deferred checks): ACCEPTED — input authorization (public
  internet-addressable sources and scratch-derived files only; local/private/credentialed
  specs are `not-run (unauthorized input)`) plus bounded recording (derived
  value/comparison and justifying lines only — the record rides into Step 2b's stdin).
- F11 (medium, coverage predicates): ACCEPTED — per-item research reading (checkable H
  beside `ledger: none` is the finer-grain defect); audit-side substantive per-target
  coverage with expected sets derived from the artifact; unresolved-column parking counts
  only for checks that could not run; token-F over a finding-rich artifact is breakage.
- F12 (medium, "confirms" classification): ACCEPTED — every executed new-check result
  returns to the auditor via delta audit; bounded termination (third round of new checks
  → `unverifiable (unstable check set)`, recorded).
- F13 (medium, delta overwrites original): ACCEPTED — ATTEMPT parameter suffixes both
  label and audit artifact; predecessors preserved; GOAL.md assumption amended.
- F14 (medium, empty brief): ACCEPTED — brief gets the full input test before allocation.
- F15 (low, scratch leak): ACCEPTED — unconditional cleanup at mktemp, gated trap swap at
  launch.
- Standing context: no-new-tests repo policy; install.sh untouched by this unit.

Consensus: disagreed
