## Carried decisions — Round 004
- F12, F13, F14, F15: verified CLOSED by round 004.
- F16 (high, fetched content executable): ACCEPTED — fetched material is INERT DATA
  (never execute/import/source/install/evaluate, however public the source);
  authorization makes an input readable, not runnable; computations always
  orchestrator-authored; no credentials in scratch runs.
- F17 (medium, F-items vanish at verdict boundary): ACCEPTED — one verdict-summary row
  per independently derived target: every enumerated H-item AND every F-item the audit
  examines; mirrored in the fallback trigger.
- F18 (low, scratch preserved on pre-launch refusal): ACCEPTED — launch-time trap removes
  scratch when no launch claim exists OR a terminal record exists. Residual: an
  unattributable foreign claim preserves scratch fail-closed.
- F19 (low, pre-contract research artifact): RECORDED FOLLOW-UP — regenerate and audit
  this Goal's research artifact through the new pipeline; non-blocking; intro already
  says evidence-informed.
- Standing context: no-new-tests repo policy; install.sh untouched by this unit.

Consensus: disagreed
